#include <stdio.h>

int main() {
    int n, i;
    float sum = 0;

    printf("Enter a positive integer: ");
    scanf("%d", &n);

    for (i = 1; i <= n; i++) {
        sum += 1.0 / i;
    }

    printf("Sum of the expression is: %f", sum);

    return 0;
}
