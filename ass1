#include <stdio.h>

int main() {
    int i, j, num = 1;

    for (i = 0; i < 3; i++) {
        for (j = 0; j < 4; j++) {
            printf("%d\t", num);
            num += 2;
        }
        num -= 6;   // reset num to the first value of the next row
        printf("\n");
    }

    return 0;
}
